## some changes in the 
    - there is dashboard on '/' page remove and navigate to landing page put dashboard on it on /farmer/dashboard

    - if logged in it on landing page instead of auth buttons it should show user profile and dashboard button 

    - http://localhost:5173/farmer/dashboard
    when a unauthorized user hits this endpoint it renders the login page and the url still remains to be http://localhost:5173/farmer/dashboard
    so change it to perticulat auth url 
    also use protected routes 

    - remove 
        1. the badge from dashboard 
        2. Schedule grain arrival slots, access direct weighbridge tokens, and track crop realizations without waiting in gate queues. this text on dashboard 
    
    - Active Unloading Bookings imporve the UI for active unloading bookings make the UI consistent straight and sleek way now it's also donnot try to make it a box inside text and stuff metioned in chat images 
