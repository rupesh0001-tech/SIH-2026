for this prompt only 
mandi = shop ( donnot rename it )

# Map & Location Feature

Add a new map/location feature to the existing React Native app.

## Goal

Show:

* User's current location on a map.
* Mock nearby shops around the user's location.
* Shop markers on the map.
* Basic location permission handling.

## Services / Libraries to Use

* **User location:** `expo-location`
* **Map:** `react-native-maps`
* **Map provider:** Google Maps provider supported by `react-native-maps`
* **Shop data:** Local mock data only for now.

Do **not** integrate Google Places, Mapbox Search, OpenStreetMap/Overpass, Nominatim, or any shop API yet.

## Important

* Work within the existing project structure.
* Do not rewrite or replace existing app architecture.
* Do not add a backend for this feature yet.
* Keep the implementation minimal and modular so real shop/location APIs can be added later.
* Follow the existing project's UI and coding conventions.


now replace the current map with this and  render tge things up 
